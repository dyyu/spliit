POSTGRES_PRISMA_URL=postgresql://postgres:1234@localhost
POSTGRES_URL_NON_POOLING=postgresql://postgres:1234@localhost

NEXT_PUBLIC_DEFAULT_CURRENCY_CODE=""

# Set to enable the operator view at /ops, protected by HTTP Basic auth.
# Leave unset and /ops returns 404. There is no rate limiting, so use a long
# random value.
OPS_PASSWORD=
